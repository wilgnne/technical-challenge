{
  "name": "technical-challenge",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/domain",
      "packages/infra",
      "packages/*"
    ]
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "dev": "yarn workspace @technical-challenge/api dev",
    "watch": "tsc -b ./tsconfig.packages.json --watch",
    "test": "yarn workspace @technical-challenge/api test"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}